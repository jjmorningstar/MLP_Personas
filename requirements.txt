# LLM_Personas — Python dependencies
#
# Executive branch (executive/*.py): stdlib only — no extra deps.
# Portal scripts: stdlib only (no pip install required).
#
# Python 3.9+ recommended. To install:
#   python3 -m venv .venv
#   source .venv/bin/activate   # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Required for running litigation/run.py and provider adapters:
pyyaml           # For YAML config parsing
# Ollama integration (for local LLMs)
ollama           # https://pypi.org/project/ollama/
# OpenAI-compatible API (for LM Studio, OpenRouter, etc.)
openai           # https://pypi.org/project/openai/
# Research Report workflow (agents/workflows/research_report.py)
python-dotenv    # Load .env for API keys
tavily-python    # Web search (primary; 1000 free credits/mo)
duckduckgo-search  # Web search fallback when TAVILY_API_KEY not set
