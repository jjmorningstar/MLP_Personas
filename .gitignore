# Environment and secrets (never commit credentials)
.env
.env.*
!.env.example

# Litigation runner config (may contain API keys)
litigation/config.yaml

# Executive branch override proof secret (never commit)
executive/.executive_secret

# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp
*.swo

# Portal generated (optional: commit transcripts_manifest.json for viewer discovery)
# Output dirs excluded to avoid large repo size / GitHub limits
courtroom/portal/exports/
courtroom/portal/output/

# Optional: uncomment if you want to ignore state backups
# state/backups/
